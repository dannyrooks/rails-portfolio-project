<!--
<nav>
    <ul>
        <li><%= link_to 'Home', root_path %></li>
        <% if !user_signed_in? %>
            <li><%= link_to 'Signup', new_user_registration_path %></li>
            <li><%= link_to 'Login', new_user_session_path %></li>
        <% else %>
            <li><%= current_user.email %></li>
            <li><%= link_to 'Logout', destroy_user_session_path, method: 'delete' %>
        <% end %>     
    </ul>
</nav> 
-->

<% if !user_signed_in? %>
<nav class="nav">
  <a class="nav-link disabled" href="#"></a>
  <a class="nav-link active" href="/">Home</a>
  <a class="nav-link" href="/locations">Locations</a>
  <a class="nav-link" href="/", delete>Logout</a>
  <% else %>
    <a class="nav-link" href="/users/sign_up">Signup</a>
    <a class="nav-link" href="/users/sign_in">Login</a>
    <% end %>
</nav>

<!--
  <body>
    <div class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <%= link_to "Fishing Report", root_path, class: "navbar-brand" %>
        </div>
        <ul class="nav navbar-nav">

        </ul>
        <% if user_signed_in? %>
         <p><%= link_to "New Location", new_location_path, class: "navbar-right navbar-text navbar-link" %></p>
         <% end %>
      </div>
    </div>
    -->

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>

  </body>
 
