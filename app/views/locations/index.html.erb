<h1>Locations</h1>

  <% @locations.each do |location| %>
    <%= link_to location.name, location_path(location), class: "btn btn-primary" %><br>
     <% end %>
    <br>
     <%= link_to "New Location", new_location_path %>